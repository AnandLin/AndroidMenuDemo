<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.anand.menudemo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".app.PopupMenuActivity"></activity>
        <activity android:name=".app.ContextMenu4Activity" />
        <activity android:name=".app.ContextMenu3Activity" />
        <activity android:name=".app.ContextMenu2Activity" />
        <activity android:name=".app.SubMenuActivity" />
        <activity android:name=".app.ContextMenuActivity" />
        <activity android:name=".app.OptionMenu2Activity" />
        <activity android:name=".app.OptionMenuActivity" />
        <activity android:name=".app.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>